{% extends 'base.html.twig' %}

{% block body %}

  <div class="container-fluid" style="margin-top:3%;">
    <div class="row">
      <div class="col-sm-4"></div>

      <div class="col-sm-4">
        {% for quote in quotes %}
          {% include 'default/_quote.html.twig' with {quote: quote} %}
        {% endfor %}
      </div>

      <div class="col-sm-4"></div>
    </div>
  </div>

{% endblock %}
